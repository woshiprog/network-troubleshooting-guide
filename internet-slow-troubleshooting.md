---
layout: default
title: "网速突然变慢怎么办？家庭网络排查顺序"
description: "网速突然变慢时，按单个服务、单台设备、Wi-Fi、有线、后台占用、路由器和运营商线路的顺序逐层排查。"
permalink: /internet-slow-troubleshooting/
---

<article class="article-page" markdown="1">

<header class="article-header">

# 网速突然变慢怎么办？

<p class="article-deck">先确认是单个网站或应用慢、单台设备慢，还是整个家庭网络都慢。影响范围不同，排查方向也完全不同。</p>

</header>

<div class="answer-box" markdown="1">

**推荐顺序：**换服务、换设备、靠近路由器、暂停后台传输，再做相同条件下的测速和网线对照。只有多台设备、有线和无线同时变慢时，才优先检查路由器上游和运营商线路。

</div>

## 先确定“哪里慢”

<div class="table-wrap" markdown="1">

| 现象 | 优先检查 | 对照方法 |
| --- | --- | --- |
| 只有一个网站或应用慢 | 服务端、目标路径、应用状态 | 更换服务或查看官方状态 |
| 只有一台设备慢 | 后台任务、信号、系统或网卡 | 同位置更换设备 |
| 远离路由器才慢 | 无线覆盖和干扰 | 靠近路由器复测 |
| 固定时段变慢 | 家庭负载或上游拥塞 | 正常与异常时段分别记录 |
| 有线和无线都慢 | 路由器、光猫或上游线路 | 多设备和网线对照 |
| 上传时全家卡顿 | 上行占满和网络排队 | 暂停上传后复测 |
| 测速正常但互动卡顿 | 延迟、抖动或丢包 | 连续测试网络质量 |
| 下载软件显示的速度比套餐数字小 | 单位不同或服务端限制 | 核对 Mbps 与 MB/s |

</div>

## 从不修改设置的检查开始

### 1. 更换网站或应用复测

下载慢时，测试另一个可信来源；视频慢时，测试另一项平时使用的服务。

- 只有一个目标慢：更可能是目标服务、服务器负载或访问路径。
- 多个无关服务都慢：继续检查设备和家庭网络。
- 网页正常但应用慢：检查应用更新、后台状态和服务端情况。
- 应用正常但网页慢：检查浏览器、扩展、代理和 DNS。

只有一个目标变慢时，不要重启整个家庭网络，也不要修改路由器设置。

### 2. 查看后台流量

检查当前设备和家庭内其他设备是否正在进行：

- 系统或游戏更新。
- 云盘同步。
- 照片和视频备份。
- 大文件下载或上传。
- 视频直播或高画质播放。
- 多台设备同时测速。
- 摄像头或其他设备持续上传。

暂停一个任务后立即恢复，说明带宽可能正在被共享使用，不需要重置网络。

测试结束后，可以恢复必要任务。不要为了得到更高的测速数字，长期关闭安全更新或备份。

### 3. 比较另一台设备

让两台设备在以下条件尽量一致时测试：

- 相同位置。
- 相同 Wi-Fi 频段。
- 相近时间。
- 相同测速服务。
- 相同测试服务器。
- 没有额外的大流量任务。

只有一台设备慢时，重点检查：

- 后台任务。
- 节能模式。
- 系统更新。
- 无线驱动。
- 浏览器或应用状态。
- 设备存储和整体性能。
- 手动代理、DNS 或固定 IP。

不要因为单台设备慢，就直接重启或重置整个家庭网络。

### 4. 靠近路由器复测

保持使用同一台设备和同一测试服务，移动到路由器附近复测。

- 靠近后明显改善：无线覆盖、墙体遮挡或干扰更可疑。
- 不同位置结果相近：继续检查设备、路由器负载和上游线路。
- 只有某个房间变慢：重点检查覆盖和干扰。
- 路由器附近仍然很慢：继续做网线和多设备对照。

路由器通常更适合放在通风、较高、相对居中并减少金属遮挡的位置。不要把设备覆盖在密闭柜体中，也不要堵住散热孔。

### 5. 做网线对照

有条件时，使用已知正常的网线，把电脑连接到路由器 LAN 口。

使用相同测速服务、相同服务器和相近时段进行测试。

- 网线稳定而 Wi-Fi 慢：无线覆盖、频段、干扰或无线设备更可疑。
- 网线和 Wi-Fi 都慢：继续检查路由器、光猫和上游线路。
- 更换 LAN 口后恢复：原端口或连接状态可能异常。
- 更换网线后恢复：原网线可能损坏或接触不良。
- 只有一台有线设备慢：检查该设备的网卡、驱动和接口。

不要在不了解配置的情况下，把电脑直接连接到运营商设备并修改上网方式。

## 怎样进行可比较的测速

一次测速只能反映当时的部分情况。为了让结果可以比较，应记录：

- 测试日期和时间。
- 使用的设备。
- Wi-Fi 或网线连接方式。
- Wi-Fi 频段。
- 设备与路由器的距离。
- 测速服务和测试服务器。
- 是否存在下载、上传或同步任务。
- 其他设备是否正在大量使用网络。

建议在相同条件下连续测试三次，保留全部结果，不要只记录最高值。

再分别记录：

- 网络正常时的一组结果。
- 实际故障时的一组结果。
- Wi-Fi 的一组结果。
- 网线的一组结果。

不要连续同时打开多个测速页面。测速本身会占用带宽，并影响其他设备。

## Mbps 和 MB/s 为什么不同

宽带套餐和测速页面通常使用 `Mbps`，部分下载软件使用 `MB/s`。

两者不是同一个单位：

```text
8 Mbps 约等于 1 MB/s
```

实际传输还会受到协议开销、服务器能力、设备性能和网络状态影响，因此不能只用简单换算判断是否存在故障。

比较前先确认页面显示的是 `Mbps`、`Mb/s`、`MB/s` 还是其他单位。

## 测速低于套餐数字是否一定有问题

不一定。

结果可能受到以下因素影响：

- 套餐标称方式。
- 测试服务器的位置和负载。
- Wi-Fi 规格与信号。
- 设备网卡能力。
- 网线和端口速率。
- 路由器性能。
- 同时使用网络的设备数量。
- 测试时段。
- 后台传输。
- 运营商线路状态。

更有价值的证据是：使用网线、相同服务器、相近条件多次测试，并比较正常与异常时段。

## 什么时候重启网络设备

只有多台设备、有线和无线都变慢，并且已经排除单个服务和后台流量时，才考虑重启光猫和路由器。

操作前：

1. 保存正在进行的工作。
2. 通知同一网络中的其他人会暂时断网。
3. 确认没有正在进行的重要上传、会议或远程操作。
4. 记录光猫和路由器当前指示灯状态。
5. 不要按住标有 Reset 的小孔按钮。

重启顺序：

1. 关闭路由器和光猫电源。
2. 等待约 20 秒。
3. 先启动光猫。
4. 等待线路相关指示灯稳定。
5. 再启动路由器。
6. 等待 Wi-Fi 和互联网连接恢复。
7. 使用相同设备和相同条件复测。

普通断电重启通常不会删除配置，但会让所有设备短暂断网。

<div class="warning" markdown="1">

**业务中断提醒：**光猫可能同时承载电话、电视、监控或其他运营商业务。不确定时，应先询问同住人员或联系运营商，避免中断正在使用的服务。单位、学校和受管网络不应自行重启网络设备。

</div>

## 网络重置应该放在最后

只有单台设备持续异常，普通重启、重新连接、后台检查和多设备对照均无效，并且已经排除全网问题时，才考虑系统提供的“网络重置”。

<div class="warning" markdown="1">

**风险与恢复：**网络重置可能删除已保存的 Wi-Fi、代理、手动地址、DNS 和其他网络组件设置，也可能要求重新安装特定网络软件。操作前记录所有自定义项目并确认 Wi-Fi 密码。恢复时重新连接网络，再按记录还原确有需要的代理、固定地址或 DNS。受管设备应交由管理员处理。

</div>

不要把网络重置和路由器恢复出厂设置混为一谈。前者主要影响当前设备，后者会清除家庭路由器的上网和无线配置。

## 根据结果采取行动

- 只有一个网站或应用慢：查看目标服务状态或稍后重试。
- 只有一台设备慢：处理后台任务、系统、浏览器和无线适配器。
- 只在弱信号位置慢：改善路由器位置或无线覆盖。
- 上传时全网卡顿：调整上传任务的时间或并发数量。
- Wi-Fi 慢而网线稳定：检查无线覆盖、频段和干扰。
- 多台设备、有线和无线在固定时段都慢：向运营商提供成组测试记录。
- 本地网关延迟也明显波动：检查设备到路由器之间的链路。
- 测速正常但语音、会议或游戏卡顿：继续测试延迟、抖动和丢包。

## 什么时候停止自行排查

<div class="stop-box" markdown="1">

**停止操作并联系专业人员：**路由器、光猫或电源适配器出现异常高温、焦味、冒烟、进水、外壳变形、反复断电或明显供电噪声时，应立即断电。光纤折断或接口损坏时，不要自行处理或清洁端面。

</div>

## 常见问题

### 重启路由器后变快，问题解决了吗？

不一定。它只能说明重启暂时清除了某种状态或负载。如果问题频繁复发，应检查散热、供电、固件、设备数量和上游线路，而不是每天依赖重启。

### 需要每天测速吗？

一般不需要。在实际异常时记录一组结果，并保留正常时段的基线，更有助于定位和报障。

### 为什么上传文件时下载和网页也会变慢？

上传占满后可能产生网络排队，使其他请求等待时间增加。暂停上传后立即恢复，可以支持这一判断，但还应结合延迟和网关测试确认。

### 为什么手机比电脑测速快？

两台设备的无线规格、天线、频段、驱动、节能策略和后台任务可能不同。应先让它们使用相同位置、频段和测试服务器，再比较结果。

<div class="next-links" markdown="1">

[理解网络质量指标]({{ '/network-quality/' | relative_url }}) [延迟和抖动测试]({{ '/network-latency-test/' | relative_url }}) [路由器异常排查]({{ '/router-network-troubleshooting/' | relative_url }})

</div>

</article>
