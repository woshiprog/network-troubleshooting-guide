---
layout: default
title: "只有一台设备无法上网怎么办？设备侧排查"
description: "同一 Wi-Fi 下只有一台手机或电脑无法上网时，逐步检查无线连接、地址、日期时间、代理、DNS 和网络重置。"
permalink: /one-device-no-internet/
---

<article class="article-page" markdown="1">

<header class="article-header">

# 只有一台设备无法上网怎么办？

<p class="article-deck">其他设备在同一网络中正常，说明路由器和上游线路大体可用。优先检查故障设备，不要重置整个家庭网络。</p>

</header>

<div class="answer-box" markdown="1">
**先做三项：**关闭再开启 Wi-Fi、重启设备、确认日期时间自动且正确。仍无效时检查设备获得的网络地址，以及是否启用了手动代理、DNS 或固定 IP。
</div>

## 症状与方向

<div class="table-wrap" markdown="1">

| 现象 | 可能原因 | 优先检查 |
| --- | --- | --- |
| 显示已连接但无网络 | 地址、网关、DNS 或代理 | 网络详情与其他设备对照 |
| 无法加入 Wi-Fi | 密码、保存记录或安全设置 | 重新输入密码 |
| 浏览器证书错误 | 日期时间不正确 | 自动日期、时间和时区 |
| 只有浏览器失败 | 扩展、缓存或代理 | 另一浏览器与无痕窗口 |
| 获得 `169.254.x.x` 地址 | 未从路由器获得地址 | 重新连接与地址分配 |
| 更新系统后出现问题 | 驱动或网络组件状态 | 系统更新与适配器状态 |

</div>

## 逐步排查设备

### 1. 重新连接并重启

关闭 Wi-Fi 约 10 秒后再打开。若无效，重启设备。飞行模式也应确认已关闭。这些步骤不删除保存的网络。

### 2. 检查日期、时间和时区

时间明显错误会导致安全证书验证失败，看起来像“有网但网页打不开”。开启系统的自动日期与时间，再重新加载网页。

### 3. 查看地址、网关与 DNS

打开该 Wi-Fi 的网络详情，与正常设备对照。两台设备的地址不能完全相同，但通常应处于相同局域网范围，并有相同网关。手动固定 IP 如果与其他设备冲突，会造成断网。

### 4. 关闭不需要的手动代理

如果你从未主动配置代理，但设备网络详情中出现手动代理地址，可先截图记录，再切回系统默认。公司或学校设备可能依赖代理，应联系管理员，不要自行关闭。

### 5. 忘记网络并重新加入

<div class="warning" markdown="1">
**风险与恢复：**“忘记此网络”会清除保存的密码及该网络的手动 IP、DNS、代理等设置。先确认 Wi-Fi 密码，并截图记录自定义项。恢复时重新选择网络、输入密码，再只还原确有需要的配置。
</div>

### 6. 更新系统或无线驱动

在有可靠网络或可离线准备更新包时，使用设备厂商或操作系统的官方更新渠道。不要从不明“驱动站”下载网络修复软件。更新前保存工作，了解是否需要重启。

### 7. 最后才做网络重置

<div class="warning" markdown="1">
**风险与恢复：**网络重置可能移除 Wi-Fi 记录、网络适配器、代理、DNS 和固定地址配置。执行前记录所有设置和 Wi-Fi 密码；执行后重启设备、重新加入 Wi-Fi，并按记录恢复必要配置。单位受管设备应交由管理员。
</div>

## 什么时候可能是硬件问题

设备在多个已知正常网络上都无法发现 Wi-Fi、连接极不稳定，或仅在紧贴路由器时有微弱信号，可能涉及天线或无线模块。若设备曾跌落、进水或维修后出现问题，应联系售后检测。

## 常见问题

### 其他设备正常，还需要重启路由器吗？

通常不需要。先处理单台设备，避免让全家断网。只有设备在其他网络正常、但在这个路由器上持续失败时，才进一步检查兼容性或地址分配。

### 忘记网络和网络重置有什么区别？

忘记网络只删除某个 Wi-Fi 的记录；网络重置影响范围更广，可能重新安装网络组件并清除多类配置，应放在最后。

### 手动设置固定 IP 能解决吗？

不建议用它绕过尚未查明的地址分配故障。填错网关、掩码或重复地址会制造新问题，应先确认路由器为什么没有正常分配地址。

<div class="next-links" markdown="1">

[Wi-Fi 已连接但不能上网]({{ '/wifi-connected-no-internet/' | relative_url }}) [DNS 异常排查]({{ '/dns-troubleshooting/' | relative_url }})

</div>
</article>

