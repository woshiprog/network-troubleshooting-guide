---
layout: default
title: "只有一台设备无法上网怎么办？设备侧排查"
description: "同一网络下只有一台手机或电脑无法上网时，逐步检查无线连接、地址、日期时间、代理、DNS、接入限制和网络重置。"
permalink: /one-device-no-internet/
---

<article class="article-page" markdown="1">

<header class="article-header">

# 只有一台设备无法上网怎么办？

<p class="article-deck">其他设备在同一网络中正常，说明路由器和上游线路整体仍然可用。应优先检查故障设备及路由器对该设备的接入状态，不要重置整个家庭网络。</p>

</header>

<div class="answer-box" markdown="1">

**先做三项：**关闭再开启 Wi-Fi、重启故障设备、确认日期和时间正确。仍然无效时，再检查设备获得的 IP 地址、网关、DNS，以及是否启用了手动代理、固定 IP 或设备接入限制。

</div>

## 症状与排查方向

<div class="table-wrap" markdown="1">

| 现象 | 可能原因 | 优先检查 |
| --- | --- | --- |
| 显示已连接但无网络 | 地址、网关、DNS、代理或接入限制 | 网络详情与正常设备对照 |
| 无法加入 Wi-Fi | 密码、保存记录或无线兼容性 | 重新输入密码并查看错误提示 |
| 浏览器显示证书错误 | 日期、时间或时区不正确 | 开启自动日期和时间 |
| 只有一个浏览器失败 | 扩展、网站数据、安全 DNS 或代理 | 另一浏览器和无痕窗口 |
| 获得 `169.254.x.x` 地址 | 没有从路由器获得正常地址 | 重新连接并检查地址分配 |
| 多个正常域名都无法解析 | DNS 或安全 DNS | 查询域名并检查解析设置 |
| 更新系统后出现问题 | 驱动、网络组件或权限状态 | 官方更新和适配器状态 |
| 路由器显示该设备被暂停 | 接入控制、黑名单或家长控制 | 核对设备状态和管理规则 |
| 在多个正常网络中都失败 | 设备系统、网卡或硬件 | 官方诊断或售后检测 |

</div>

## 逐步排查设备

### 1. 确认确实只有一台设备异常

让另一台手机或电脑连接同一个 Wi-Fi，并打开两个平时可以访问的网站。

- 另一台设备正常：继续检查故障设备。
- 另一台设备也失败：转到路由器或上游线路排查。
- 只有一个网站失败：检查网站、浏览器或 DNS。
- 故障设备在移动数据或其他网络中正常：当前 Wi-Fi 的设备接入或兼容性更可疑。
- 故障设备在多个正常网络中都失败：设备系统或硬件更可疑。

不要只用一个应用判断设备是否完全断网，因为应用服务自身也可能发生故障。

### 2. 重新连接并重启设备

关闭 Wi-Fi，等待约 10 秒后重新开启。

同时确认：

- 飞行模式已经关闭。
- 设备没有持续在 Wi-Fi 和移动网络之间切换。
- 当前连接的是正确的 Wi-Fi 名称。
- Wi-Fi 密码没有近期变更。
- 设备没有显示需要登录或认证的提示。

仍然无效时，可以重启设备。关闭再开启 Wi-Fi 和普通重启不会删除已经保存的网络密码。

### 3. 检查日期、时间和时区

设备时间明显错误时，安全证书验证可能失败，看起来像“已经联网但网页打不开”。

开启系统提供的自动日期、自动时间和自动时区，再重新访问多个网站。

如果只有一个网站显示证书错误，不要直接修改设备时间以外的安全设置，也不要忽略警告输入账号或支付信息。

### 4. 查看 IP 地址、网关与 DNS

打开当前 Wi-Fi 的网络详情，并与一台正常设备进行对照。

需要注意：

- 两台设备的 IP 地址不应该完全相同。
- 家庭网络中的设备通常处于相同的局域网范围。
- 正常设备通常会显示有效的默认网关。
- 手动固定 IP 如果与其他设备重复，可能造成断网。
- 错误的网关、子网掩码或 DNS 也可能阻断连接。
- `169.254.x.x` 一类地址通常说明设备没有获得正常的 IPv4 局域网地址。

如果故障设备使用手动 IP，而其他设备使用自动获取，应先截图记录原设置，再考虑恢复为自动获取进行测试。

单位、学校和其他受管设备可能必须使用固定地址，不应自行修改。

### 5. 检查代理和安全 DNS

如果从未主动配置代理，但设备网络详情中出现手动代理地址，应先截图记录，并确认它是否来自单位、学校、安全软件或其他管理配置。

个人设备中确认不需要该代理时，可以恢复为系统默认状态后复测。

如果只有浏览器失败，还应检查浏览器是否单独启用了：

- 手动代理。
- 安全 DNS。
- 网络过滤扩展。
- 最近新增的浏览器扩展。
- 特定网站权限。

公司、学校或受管设备可能依赖代理和安全 DNS，应联系管理员，不要自行关闭。

### 6. 检查路由器是否限制了这台设备

在你拥有家庭路由器管理权限时，可以查看设备列表和接入控制状态，但先不要修改其他网络设置。

检查是否存在：

- 暂停上网。
- 黑名单或拒绝接入。
- 家长控制时间表。
- 访客网络设备隔离。
- 设备数量限制。
- 针对该设备的访问规则。
- 路由器未能为该设备分配地址。

修改前截图记录原状态。只调整与故障设备直接相关的项目，不要关闭整个家庭网络的安全功能。

部分手机和电脑会使用随机或私有硬件地址。如果路由器规则绑定了旧的设备地址，路由器可能把同一设备识别为新设备。不要随意关闭隐私地址功能，应先核对路由器中的设备名称、当前地址和原有规则。

### 7. 忘记网络并重新加入

只有其他设备正常，并且当前设备反复连接失败时，再考虑使用“忘记此网络”。

<div class="warning" markdown="1">

**风险与恢复：**“忘记此网络”会清除保存的 Wi-Fi 密码，以及该网络的手动 IP、DNS、代理和其他自定义设置。操作前确认 Wi-Fi 密码，并截图记录所有自定义项目。恢复时重新选择网络、输入密码，再只还原确有需要的设置。

</div>

忘记网络后：

1. 等待约 10 秒。
2. 重新选择正确的 Wi-Fi 名称。
3. 输入确认无误的密码。
4. 等待设备获得网络地址。
5. 测试多个网站和应用。
6. 检查是否仍然出现相同错误。

不要连续多次忘记和重新添加网络，这不会修复路由器接入限制或硬件问题。

### 8. 更新系统或无线驱动

如果问题在系统更新后出现，或设备在多个网络中持续异常，可以检查操作系统和无线驱动状态。

只使用：

- 操作系统自带的更新功能。
- 设备厂商官方网站。
- 设备厂商提供的管理程序。
- 管理员明确提供的更新方式。

不要从陌生“驱动站”、弹窗或错误页面下载网络修复软件。

更新前保存工作，并确认：

- 设备有可靠电源。
- 更新包适用于当前型号和系统版本。
- 是否需要重启。
- 是否有恢复或回退方法。

受管设备应由管理员处理。

### 9. 最后才做网络重置

只有以下条件都满足时，才考虑系统提供的“网络重置”：

- 只有这一台设备异常。
- 普通重启无效。
- 重新连接和忘记网络无效。
- 地址、代理、DNS 和路由器接入状态已经检查。
- 设备在多个正常网络中仍然异常。
- 已经记录所有自定义网络设置。

<div class="warning" markdown="1">

**风险与恢复：**网络重置可能移除 Wi-Fi 记录、网络适配器、代理、DNS、固定地址和其他网络组件设置。执行前记录所有设置和 Wi-Fi 密码。执行后重启设备、重新加入 Wi-Fi，并按记录恢复必要配置。单位、学校和其他受管设备应交由管理员处理。

</div>

网络重置影响的是当前设备，不等于恢复路由器出厂设置。不要因为一台设备异常而重置整个家庭路由器。

## 根据结果选择下一步

- 关闭再开启 Wi-Fi 后恢复：可能是临时连接或地址更新问题。
- 校准时间后恢复：问题可能来自证书验证。
- 恢复自动地址后正常：原手动 IP、网关或 DNS 设置可能有误。
- 关闭不需要的代理后正常：原代理不可达或配置错误。
- 解除设备暂停后正常：问题来自路由器接入规则。
- 忘记网络后恢复：原保存记录或该网络的设备配置可能异常。
- 在其他网络正常、当前网络持续失败：检查路由器兼容性、地址分配和接入规则。
- 在多个正常网络中都失败：继续检查系统、无线网卡或硬件。

## 什么时候可能是硬件问题

出现以下情况时，设备硬件问题的可能性上升：

- 在多个已知正常网络中都无法发现 Wi-Fi。
- 连接极不稳定，并且其他设备正常。
- 只有紧贴路由器时才能收到微弱信号。
- 设备曾经跌落、进水或拆修。
- 无线功能反复自行关闭。
- 系统无法识别无线适配器。
- 官方诊断持续报告无线硬件异常。

应联系设备售后进行检测，不要自行拆机。

<div class="stop-box" markdown="1">

**停止自行操作：**设备出现异常高温、焦味、冒烟、进水、电池鼓包或外壳损坏时，应立即停止使用并断开电源。不要继续充电、拆机或通过反复重置尝试修复。

</div>

## 常见问题

### 其他设备正常，还需要重启路由器吗？

通常不需要。先处理单台设备，避免让所有人断网。只有该设备在其他网络正常，但在当前路由器上持续失败时，才进一步检查兼容性、地址分配和接入规则。

### 忘记网络和网络重置有什么区别？

忘记网络只删除某个 Wi-Fi 的保存记录；网络重置影响范围更广，可能重新安装网络组件并清除多类配置，因此应放在最后。

### 手动设置固定 IP 能解决吗？

不建议用固定 IP 绕过尚未查明的地址分配故障。填错网关、掩码或重复地址会制造新问题，应先确认路由器为什么没有正常分配地址。

### 为什么路由器把同一台手机显示成新设备？

部分设备会使用随机或私有硬件地址。不同网络或设置变化可能让路由器看到不同的设备地址。应先核对当前设备信息和路由器规则，不要直接关闭整个网络的接入控制。

<div class="next-links" markdown="1">

[Wi-Fi 已连接但不能上网]({{ '/wifi-connected-no-internet/' | relative_url }}) [DNS 异常排查]({{ '/dns-troubleshooting/' | relative_url }}) [网页打不开排查]({{ '/website-not-opening/' | relative_url }})

</div>

</article>
