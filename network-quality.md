---
layout: default
title: "网络质量怎么看：速度、延迟、抖动与丢包"
description: "解释下载速度、上传速度、延迟、抖动和丢包的区别，并给出可以重复比较的家庭网络测试方法。"
permalink: /network-quality/
---

<article class="article-page" markdown="1">

<header class="article-header">

# 网络质量怎么看：速度、延迟、抖动与丢包

<p class="article-deck">一次测速不能概括全部网络质量。视频和下载更容易受到持续传输能力影响，语音、会议、游戏和其他实时互动则更怕延迟波动与丢包。</p>

</header>

<div class="answer-box" markdown="1">

**直接判断：**先明确遇到的是“加载慢”“反应慢”“忽快忽慢”还是“偶尔中断”，再选择速度、延迟、抖动或丢包测试。不要只看测速页面中的最大数字。

</div>

## 五个指标分别回答什么

<div class="table-wrap" markdown="1">

| 指标 | 反映的问题 | 常见使用感受 | 测试重点 |
| --- | --- | --- | --- |
| 下载速度 | 数据持续到达设备的能力 | 下载、更新、视频加载慢 | 同一服务器多次测试 |
| 上传速度 | 数据持续从设备发出的能力 | 上传、直播、会议画面受影响 | 暂停后台同步后复测 |
| 延迟 | 一次请求往返所需时间 | 点击反应慢、实时操作滞后 | 比较本地网关与外部目标 |
| 抖动 | 多次延迟的变化程度 | 声音断续、操作节奏不稳 | 连续测试而非单次值 |
| 丢包 | 测试请求没有正常收到回应的比例 | 卡顿、重传、短暂掉线 | 本地网关与外部目标对照 |

</div>

这些指标没有适用于所有网络、所有设备和所有应用的统一合格线。

测试结果会受到以下因素影响：

- 测试目标的距离。
- 网络路径。
- Wi-Fi 或网线连接方式。
- 测试时段。
- 设备性能。
- 路由器性能。
- 同时使用网络的设备数量。
- 后台上传和下载。
- 测试服务器负载。
- 目标是否限制测试回应。

相同条件下的前后对照，通常比孤立数字更有价值。

## 先根据使用感受选择测试

### 加载或下载持续很慢

优先检查：

- 下载速度。
- 上传速度是否被占满。
- 后台传输。
- Wi-Fi 信号和频段。
- 测试服务器。
- 有线和无线差异。

### 点击后反应很慢

优先检查：

- 本地网关延迟。
- 外部目标延迟。
- 正常和异常时段差异。
- 是否存在上传或下载排队。

### 偶尔突然卡一下

优先检查：

- 连续延迟变化。
- 最大值是否频繁跳高。
- 是否伴随请求超时。
- 卡顿是否与后台任务同步。

### 语音断续或实时互动跳动

优先检查：

- 延迟波动。
- 丢包。
- Wi-Fi 与网线差异。
- 本地网关与外部目标差异。
- 上传任务和网络排队。

### 只有一个网站或应用异常

优先检查：

- 目标服务状态。
- 另一网站或应用。
- 另一台设备。
- 另一可信网络。
- 浏览器、DNS 或访问路径。

只有一个目标异常时，不要直接重置整个家庭网络。

## 一次可以重复比较的测试

### 1. 记录测试条件

测试前记录：

- 日期和时间。
- 使用的设备。
- Wi-Fi 或网线。
- Wi-Fi 频段。
- 设备与路由器的距离。
- 测试服务和服务器。
- 当前是否存在上传、下载或同步。
- 其他设备是否正在大量使用网络。
- 实际出现问题的应用或场景。

### 2. 不要为了数字关闭所有日常服务

可以暂停明显的大文件下载、云同步和系统更新，判断它们是否影响结果。

但不要为了让测速数字更高而长期关闭：

- 安全更新。
- 必要备份。
- 家庭正常使用的设备。
- 日常必须运行的服务。

测试的目标是解释真实使用问题，不是得到最好看的数字。

### 3. 在相同条件下连续测试

使用同一设备、相同位置、相同连接方式和相同服务器连续测试三次。

记录全部结果，不要只保留最高值。

如果三次差异很大，应继续检查：

- Wi-Fi 干扰。
- 后台任务。
- 测试服务器负载。
- 路由器负载。
- 网络是否处于异常时段。

### 4. 换一台设备复测

让另一台设备在相同位置、相同频段和相近时间使用同一测试服务。

- 两台设备趋势一致：家庭网络或上游问题的可能性更高。
- 只有一台设备异常：重点检查该设备、网卡、驱动和后台任务。
- 手机与电脑差异明显：核对无线规格、频段和节能设置。

### 5. 做网线对照

有条件时，使用已知正常的网线，把电脑连接到路由器 LAN 口。

- 网线稳定、Wi-Fi 异常：无线覆盖、干扰、频段或无线网卡更可疑。
- 网线和 Wi-Fi 都异常：继续检查路由器、光猫和上游线路。
- 更换网线或端口后恢复：原线缆或端口可能存在问题。

### 6. 比较正常和异常时段

在网络使用正常时记录一组结果，再在实际故障发生时记录一组。

观察：

- 速度是否持续下降。
- 延迟是否持续升高。
- 最大延迟是否频繁跳高。
- 是否出现请求超时。
- 多台设备是否同时异常。
- 有线和无线是否同时变化。
- 问题是否总在固定时段出现。

## 怎样解释组合结果

<div class="table-wrap" markdown="1">

| 测试组合 | 更可能的方向 |
| --- | --- |
| 速度低，但延迟和丢包稳定 | 后台流量、无线能力、服务器或线路容量 |
| 速度正常，但实时互动卡顿 | 延迟波动、丢包、应用服务器或特定路径 |
| 靠近路由器后明显改善 | 无线覆盖、墙体遮挡或干扰 |
| 网线正常但 Wi-Fi 异常 | 无线频段、信号、干扰或无线网卡 |
| 网线和 Wi-Fi 同时变差 | 路由器、光猫、家庭负载或上游线路 |
| 上传时延迟明显升高 | 上行占用和网络排队 |
| 本地网关也明显波动 | 设备、Wi-Fi、网线、端口或路由器 |
| 本地网关稳定，多个外部目标异常 | 路由器上游、运营商或公共网络路径 |
| 只有一个外部目标异常 | 目标服务、回应策略或特定路径 |
| 只有一台设备异常 | 原设备、驱动、后台任务或接入规则 |

</div>

## 测速结果的常见限制

### 测速服务器不等于实际服务

测速页面通常会选择距离较近或当时较合适的服务器。

实际网站、会议、游戏或视频服务可能使用完全不同的服务器和网络路径。因此，测速很快不代表所有目标都一定正常。

### 测速本身会占用带宽

测速期间会产生大量传输，可能让其他设备暂时变慢。

不要：

- 同时打开多个测速页面。
- 在重要会议或上传期间反复测速。
- 用高频自动测试长期占用家庭网络。
- 只保留最高结果。

### 普通 Ping 只能粗略观察波动

普通 Ping 的最小值、最大值和平均值可以帮助观察变化，但不能替代专业工具计算的精确抖动指标。

单次请求超时也不能直接证明实际应用发生了相同比例的丢包。

### Mbps 与 MB/s 不是同一单位

宽带套餐和测速页面通常使用 `Mbps`，下载软件可能使用 `MB/s`。

```text
8 Mbps 约等于 1 MB/s
```

实际传输还存在协议开销，因此不能只用简单换算判断故障。

## 怎样记录运营商能够使用的信息

需要报障时，可以整理：

- 故障日期和时间。
- 受影响设备数量。
- Wi-Fi 和网线结果。
- 测速服务与服务器。
- 三次测试的完整结果。
- 正常与异常时段对照。
- 本地网关测试摘要。
- 多个外部目标测试摘要。
- 光猫和路由器指示灯状态。
- 实际出现问题的应用场景。
- 是否更换过网线和端口。

不要只提供一张最高或最低测速截图。成组结果更容易说明问题范围。

## 常见问题

### 测速一次很快，为什么使用时仍然卡？

测速持续时间较短，也可能选择较近的服务器。实际服务的网络路径、持续负载和实时波动不同，应在故障发生时对目标场景进行连续观察。

### 手机和电脑的测速结果为什么不同？

无线规格、天线、频段、节能策略、浏览器和后台任务都可能不同。先让两台设备使用相同频段、相同位置和相同测试服务，再比较趋势。

### 应该先测速度还是先 Ping？

下载或加载持续很慢时，先测速度；点击反应慢、语音断续或实时操作跳动时，先做连续延迟和丢包测试。

### 丢包率为 0 就代表网络完全正常吗？

不一定。测试可能没有覆盖故障时段，目标也可能与实际应用使用不同路径。还应结合延迟波动、速度、设备范围和真实使用体验。

### 为什么晚上固定时间变慢？

可能与家庭内同时使用网络的设备增加有关，也可能与上游线路高峰有关。应在正常和异常时段分别记录有线、无线及多设备结果，再判断问题范围。

<div class="next-links" markdown="1">

[测试延迟和抖动]({{ '/network-latency-test/' | relative_url }}) [测试网络丢包]({{ '/packet-loss-test/' | relative_url }}) [排查网速变慢]({{ '/internet-slow-troubleshooting/' | relative_url }})

</div>

</article>
