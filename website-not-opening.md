---
layout: default
title: "网页打不开怎么办？判断浏览器、DNS 还是网站故障"
description: "网页打不开时，通过换网站、换浏览器、查看错误提示和测试域名解析，判断问题来自浏览器、DNS、网络还是网站本身。"
permalink: /website-not-opening/
---

<article class="article-page" markdown="1">

<header class="article-header">

# 网页打不开怎么办？判断浏览器、DNS 还是网站故障

<p class="article-deck">先测试其他网站和另一台设备。只坏一个网站时，不要重置整个网络；所有网站都打不开时，再检查网络连接与域名解析。</p>

</header>

<div class="answer-box" markdown="1">

**两分钟判断：**在同一浏览器中打开两个不同网站，再用另一浏览器或设备访问原网站。只有原网站失败，多半是网站或访问路径问题；只有一个浏览器失败，优先检查浏览器；所有网站都失败，再检查网络、公共 Wi-Fi 认证和 DNS。

</div>

## 症状对照表

<div class="table-wrap" markdown="1">

| 测试结果 | 可能范围 | 优先动作 |
| --- | --- | --- |
| 只有一个网站打不开 | 网站维护、目标服务故障、特定路径 | 稍后重试并查看官方状态信息 |
| 只有一个浏览器打不开 | 扩展、网站数据、代理或浏览器配置 | 使用无痕窗口，检查最近新增的扩展 |
| 同一网络的所有设备都打不开该网站 | 网站、解析服务或网络路径 | 换网络对照，保留完整错误信息 |
| 所有网站都显示名称解析错误 | DNS 或加密 DNS | 查询域名并检查 DNS 设置 |
| 所有网站都连接超时 | 本地网络、路由器或上游线路 | 测试其他应用和设备 |
| 公共 Wi-Fi 已连接但网页均打不开 | 登录认证尚未完成 | 查看系统登录提示或重新连接 |
| 出现证书或时间错误 | 系统时间、证书或受控网络 | 校准时间，不要忽略安全警告 |

</div>

## 逐步排查

### 1. 保存完整错误提示

记录完整网址、发生时间和浏览器显示的错误代码。

“名称无法解析”“连接超时”“连接被拒绝”和“证书错误”指向不同环节。截图和错误代码比单纯描述“打不开”更有助于判断。

不要在错误页面中下载所谓的证书修复工具、网络加速工具或浏览器修复程序。

### 2. 测试两个其他网站

选择两个平时访问过、彼此无关的网站。

- 其他网站正常：网络整体仍然可用，重点检查原网站或访问路径。
- 所有网站都失败：继续测试其他应用、设备和网络。
- 网站能打开但图片或登录功能失败：可能是子域名、脚本、扩展或网站局部服务异常。

如果只有一个目标网站失败，不需要重启光猫，也不要恢复路由器出厂设置。

### 3. 换浏览器或使用无痕窗口

无痕窗口通常不会继续使用大部分现有会话数据，适合判断网站数据和扩展是否产生影响，但不同浏览器的无痕模式行为并不完全相同。

如果无痕窗口正常：

1. 先禁用最近安装或更新的扩展。
2. 清理该网站自己的 Cookie 和网站数据。
3. 检查该网站的弹窗、脚本或内容权限。
4. 不需要一开始就清空全部浏览历史和所有网站数据。

如果只有一个浏览器失败，也应检查该浏览器是否单独启用了代理、安全 DNS 或特殊网络扩展。

### 4. 换设备和网络做交叉测试

- 同一 Wi-Fi 下另一台设备正常：问题更可能在原设备或浏览器。
- 同一 Wi-Fi 下所有设备失败，但移动数据正常：问题可能在当前网络、解析服务或访问路径。
- 不同网络都失败：网站自身异常的可能性上升。
- 只有单位或学校网络失败：可能涉及受管网络策略，应联系管理员。

切换网络时不要进行敏感登录或支付测试。只需确认普通公开页面能否打开。

### 5. 检查公共 Wi-Fi 是否需要登录

酒店、机场、商场、医院和其他公共 Wi-Fi 可能要求先完成网页认证。设备虽然显示“已连接”，但认证完成前不一定可以访问互联网。

可以按以下顺序检查：

1. 查看系统是否弹出“登录网络”或“需要认证”的通知。
2. 断开该 Wi-Fi 后重新连接，等待系统再次显示认证入口。
3. 确认连接的是场所公开标示的正确网络名称。
4. 无法确认登录页面真伪时，向现场工作人员核对。

不要在来历不明的认证页面输入邮箱主密码、支付信息或与该场所无关的重要账号。公共网络要求安装陌生证书、配置文件或应用时，应停止操作并向网络提供方确认。

### 6. 检查域名解析

错误提示包含“找不到服务器”“无法解析主机”或类似文字时，可以执行只读查询：

```text
Windows: nslookup example.com
macOS / Linux: nslookup example.com
```

把 `example.com` 换成打不开的网站域名，不要包含 `https://` 和后面的页面路径。

查询返回地址只表示这次域名解析得到结果，不保证网站服务器、证书和访问路径一定正常。连续查询多个正常域名都失败时，再进入 [DNS 异常排查]({{ '/dns-troubleshooting/' | relative_url }})。

部分浏览器可能使用自己的“安全 DNS”或加密 DNS。`nslookup` 正常但只有一个浏览器失败时，还应检查该浏览器的安全 DNS、代理和扩展设置。

### 7. 核对系统时间与网址

出现证书错误时，先确认设备的日期、时间和时区正确。

同时检查：

- 域名是否拼写正确。
- 页面是否被跳转到陌生域名。
- 证书错误是否只发生在一个网站。
- 单位网络是否使用管理员明确提供的安全检查方案。

不要在无法确认原因时点击“继续访问”，更不要在证书警告页面中输入账号、验证码、银行卡或支付信息。

## 不建议先做的事

- 不要因为一个网站失败就恢复路由器出厂设置。
- 不要忽略证书警告并继续输入敏感信息。
- 不要从错误页面下载陌生修复工具。
- 不要同时修改 DNS、代理、浏览器和路由器。
- 不要为了测试而关闭系统安全防护或安装陌生证书。
- 不要把搜索结果中存在该网页，当成网站当前一定正常的证据。

## 什么时候联系谁

- 只有一个网站在多个网络中都失败：联系网站支持或等待其恢复。
- 单一浏览器持续失败：更新浏览器，检查扩展、安全 DNS 和网站权限。
- 公共 Wi-Fi 无法弹出认证页面：联系场所工作人员或网络提供方。
- 单位网络中的特定网站被策略阻止：联系网络管理员。
- 所有设备和所有网站都连接超时：查看 [路由器网络异常排查]({{ '/router-network-troubleshooting/' | relative_url }}) 或联系运营商。
- 出现可疑跳转、陌生证书安装要求或账号风险提示：停止输入信息，并改用可信网络核查。

## 常见问题

### 清缓存能解决所有网页问题吗？

不能。缓存只与部分加载、登录或旧资源问题有关。名称解析失败、网络中断、证书错误和网站服务异常不会因为清缓存自动解决。

### 能搜到网页但点开失败，说明 DNS 正常吗？

不一定。搜索结果可能来自搜索引擎之前保存的索引。点击后的目标域名、子域名、证书或网络路径仍可能出现问题。

### 显示连接不安全还可以继续吗？

无法确认原因时不要继续，更不要输入敏感信息。先校准系统时间、核对网址，并通过网站官方渠道确认是否存在证书故障。

### 为什么应用正常，网页却打不开？

应用和网页可能使用不同的服务器、域名、解析方式和网络路径。某个应用正常，只能说明网络并非完全中断，不能证明浏览器、DNS 和目标网站都正常。

<div class="next-links" markdown="1">

[DNS 异常怎么判断]({{ '/dns-troubleshooting/' | relative_url }}) [Wi-Fi 已连接但不能上网]({{ '/wifi-connected-no-internet/' | relative_url }}) [只有一台设备不能上网]({{ '/one-device-no-internet/' | relative_url }})

</div>

</article>
