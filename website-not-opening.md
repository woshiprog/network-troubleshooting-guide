---
layout: default
title: "网页打不开怎么办？判断浏览器、DNS 还是网站故障"
description: "网页打不开时，通过换网站、换浏览器、查看错误提示和测试域名解析，判断问题来自浏览器、DNS、网络还是网站本身。"
permalink: /website-not-opening/
---

<article class="article-page" markdown="1">

<header class="article-header">

# 网页打不开怎么办？判断浏览器、DNS 还是网站故障

<p class="article-deck">先测试其他网站和另一台设备。只坏一个网站时，不要重置整个网络；所有网站都打不开时，再检查连接与域名解析。</p>

</header>

<div class="answer-box" markdown="1">
**两分钟判断：**同一浏览器打开两个不同网站，再用另一浏览器或设备访问原网站。只有原网站失败，多半是网站或访问路径问题；只有一个浏览器失败，优先检查浏览器；所有网站都失败，再检查网络和 DNS。
</div>

## 症状对照表

<div class="table-wrap" markdown="1">

| 测试结果 | 可能范围 | 优先动作 |
| --- | --- | --- |
| 只有一个网站打不开 | 网站维护、目标服务故障、特定路径 | 稍后重试并查看官方状态信息 |
| 只有一个浏览器打不开 | 扩展、缓存、代理或浏览器配置 | 无痕窗口、禁用最近新增扩展 |
| 同一网络所有设备都打不开该网站 | 网站或网络路径 | 换网络对照，保留错误信息 |
| 所有网站都显示名称解析错误 | DNS | 查询域名并检查 DNS 设置 |
| 所有网站都超时 | 本地网络、路由器或上游线路 | 测试其他应用和设备 |
| 出现证书或时间错误 | 系统时间、证书或受控网络 | 校准时间，不要忽略安全警告 |

</div>

## 逐步排查

### 1. 保存完整错误提示

记录网址、发生时间和浏览器显示的错误代码。名称解析失败、连接超时、连接被拒绝和证书错误指向不同环节，截图比“打不开”更利于后续判断。

### 2. 测试两个其他网站

选择你平时访问过、彼此无关的网站。如果它们正常，网络整体可用，不必重启光猫或恢复路由器设置。

### 3. 换浏览器或无痕窗口

无痕窗口通常不会使用大部分现有会话数据，适合判断缓存和扩展影响。若无痕窗口正常，可先禁用最近安装的扩展，再清理该网站的数据；不需要清空所有浏览历史。

### 4. 换设备和网络做交叉测试

- 同一 Wi-Fi 下另一台设备正常：问题在原设备或浏览器。
- 同一 Wi-Fi 下所有设备失败，但移动数据正常：问题可能在当前网络、解析服务或访问路径。
- 不同网络都失败：网站自身异常的可能性上升。

### 5. 检查域名解析

错误提示包含“找不到服务器”“无法解析主机”时，可执行只读查询：

```text
Windows: nslookup example.com
macOS / Linux: nslookup example.com
```

把 `example.com` 换成打不开的网站域名，不要包含 `https://` 和后面的路径。查询返回地址并不保证网站一定正常，但完全无法解析时可进入 [DNS 异常排查]({{ '/dns-troubleshooting/' | relative_url }})。

### 6. 核对系统时间与地址

证书错误时先确认日期、时间和时区正确。检查网址拼写，尤其不要从不明提示页点击“继续访问”或安装所谓证书修复程序。

## 不建议先做的事

- 不要因为一个网站失败就恢复路由器出厂设置。
- 不要忽略证书警告并输入账号或支付信息。
- 不要从错误页下载陌生修复工具。
- 不要同时修改 DNS、代理、浏览器和路由器，多个变量会让结果无法解释。

## 什么时候联系谁

- 只有一个网站在多个网络都失败：联系网站支持或等待其恢复。
- 单一浏览器持续失败：更新浏览器，检查扩展和该网站权限。
- 单位网络中特定网站被策略阻止：联系网络管理员。
- 所有设备、所有网站都超时：查看 [路由器网络异常排查]({{ '/router-network-troubleshooting/' | relative_url }}) 或联系运营商。

## 常见问题

### 清缓存能解决所有网页问题吗？

不能。缓存只与一部分加载、登录或旧资源问题有关。名称解析、网络中断、证书错误和网站宕机不会因为清缓存自动解决。

### 能搜到网页但点开失败，说明 DNS 正常吗？

不一定。搜索结果可能来自之前的索引，点击后的目标域名、子域名或网络路径仍可能出现问题。

### 显示连接不安全还可以继续吗？

不要在无法确认原因时继续，更不要输入敏感信息。先校准系统时间、核对网址，并等待网站修复有效证书。

<div class="next-links" markdown="1">

[DNS 异常怎么判断]({{ '/dns-troubleshooting/' | relative_url }}) [Wi-Fi 已连接但不能上网]({{ '/wifi-connected-no-internet/' | relative_url }})

</div>
</article>

