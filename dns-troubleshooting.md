---
layout: default
title: "DNS 异常怎么判断？解析失败与网页打不开排查"
description: "通过错误提示、nslookup 查询、设备对照和网络对照判断 DNS 是否异常，并安全测试、修改和恢复 DNS 设置。"
permalink: /dns-troubleshooting/
---

<article class="article-page" markdown="1">

<header class="article-header">

# DNS 异常怎么判断？

<p class="article-deck">DNS 负责把域名转换为网络地址。网页打不开不一定是 DNS 问题；只有错误现象、查询结果和设备对照都指向解析环节时，才值得修改 DNS 设置。</p>

</header>

<div class="answer-box" markdown="1">

**判断要点：**多个正常域名都无法解析、`nslookup` 查询失败，而设备仍然保持有效的网络连接，才更像 DNS 异常。只有一个网站失败、查询可以返回地址或所有网络连接都中断时，应先检查其他环节。

</div>

## 哪些现象与 DNS 更相关

<div class="table-wrap" markdown="1">

| 现象 | DNS 相关性 | 判断理由 |
| --- | --- | --- |
| 多个网站提示找不到服务器或名称无法解析 | 较高 | 多个域名同时无法完成解析 |
| `nslookup` 查询多个正常域名均超时 | 较高 | 当前查询服务器不可达或没有响应 |
| 查询返回 `NXDOMAIN` | 需要核对 | 域名可能不存在、输入错误或被错误解析 |
| 只有一个网站打不开 | 较低 | 更可能是网站、证书或特定路径问题 |
| Wi-Fi 已断开或设备没有有效地址 | 很低 | 基础网络连接尚未建立 |
| 查询返回地址，但网页连接超时 | 较低 | 解析已经完成，问题可能发生在连接阶段 |
| 只有一个浏览器失败 | 较低 | 更像浏览器配置、安全 DNS、扩展或代理问题 |
| 同一网络的多台设备同时无法解析 | 较高 | 路由器提供的 DNS 或上游解析服务更可疑 |

</div>

## 先做不会修改设置的测试

### 1. 确认不是完整断网

先检查设备是否仍然连接到 Wi-Fi 或网线，并用另一台设备测试同一网络。

- 所有设备和所有应用都无法联网：先检查路由器、光猫和上游线路。
- 应用可以联网，但多个网页提示名称解析错误：继续检查 DNS。
- 只有一个网站失败：先检查网站自身或访问路径。
- 只有一个浏览器失败：先检查该浏览器的安全 DNS、扩展和代理。

完整断网时，修改 DNS 通常不会解决基础连接问题。

### 2. 查询两个不同域名

在 Windows 命令提示符、macOS 终端或 Linux 终端中运行：

```text
nslookup example.com
nslookup example.org
```

`nslookup` 是查询操作，不会修改网络设置。

结果可以这样理解：

- 返回一个或多个地址：DNS 至少完成了这次查询。
- 显示超时：当前查询服务器可能不可达、响应过慢或被网络阻断。
- 返回 `NXDOMAIN` 或“域名不存在”：先检查域名拼写，再用另一个正常域名复测。
- 只查询一个域名失败：不能据此判断整个 DNS 服务异常。
- 多个正常域名连续失败：DNS 异常的可能性上升。

不要把 `example.com` 替换成包含 `https://`、斜杠或页面路径的完整网址，只填写域名。

### 3. 用另一台设备对照

让另一台设备连接同一个网络，并查询或访问相同域名。

- 另一台设备正常：重点检查原设备的 DNS、代理、安全软件或浏览器设置。
- 两台设备都失败：路由器提供的 DNS 或上游解析服务更可疑。
- 只有一个浏览器失败：检查该浏览器是否单独启用了安全 DNS。
- 单位设备失败：先联系管理员，不要修改受管网络配置。

### 4. 换一个可信网络对照

同一设备切换到另一个可信网络后恢复，说明原网络的解析配置或上游服务更可疑。

公共网络和单位网络可能有自己的域名解析、登录认证或访问策略。不要为了绕开组织规则擅自修改受管设备或路由器设置。

切换网络只用于普通页面测试，不要在陌生网络中输入敏感账号或支付信息。

## 注意浏览器的安全 DNS

部分浏览器可能使用“安全 DNS”、DNS over HTTPS 或其他加密 DNS 功能。它的查询路径可能与系统使用的 DNS 不完全相同。

因此可能出现以下情况：

- `nslookup` 正常，但一个浏览器仍然无法打开网页。
- 一个浏览器正常，另一个浏览器提示名称解析错误。
- 系统应用可以联网，但浏览器中的部分域名失败。

遇到这种情况，应检查故障浏览器的安全 DNS、代理和扩展设置。不要因为浏览器单独失败就直接修改整个路由器的 DNS。

如果设备由公司或学校管理，应保持管理员要求的安全 DNS 或解析策略。

## 修改 DNS 前先记录原设置

只有测试确实指向 DNS，并且你有权管理该设备或家庭网络时，才考虑临时更换解析服务器进行对照。

<div class="warning" markdown="1">

**风险与恢复：**填写错误的 DNS 地址可能让所有域名都无法解析。单位网络、家长控制、内部域名和部分运营商服务也可能依赖指定 DNS。修改前截图记录原值，并注明原来是“自动获取”还是手动地址。测试结束后应恢复原设置；原来自动获取的，就重新选择“自动”。

</div>

诊断时建议先只修改一台个人设备，不要直接修改路由器。这样影响范围更小，也更容易比较修改前后的结果。

修改后按以下顺序复测：

1. 再次查询之前测试的两个域名。
2. 访问多个彼此无关的网站。
3. 检查原来失败的应用或浏览器。
4. 记录修改前后的结果。
5. 没有改善时，立即恢复原设置。

不要在修改 DNS 的同时继续修改代理、固定 IP、浏览器和路由器，否则无法判断是哪一项产生了影响。

## 结果怎么解释

- 更换后查询和网页访问都恢复：原解析服务或到它的路径可能异常，但仍应稍后恢复原设置复测，排除短暂波动。
- 查询恢复但网页仍然超时：DNS 已经不是主要阻塞点，应继续检查浏览器、目标网站、证书或网络路径。
- 更换后没有变化：恢复原设置，检查设备网络、路由器和上游线路。
- 只有一个浏览器仍然失败：检查该浏览器的安全 DNS、代理和扩展。
- 只有内部网站失效：立即恢复单位或组织要求的 DNS，并联系管理员。
- 多台设备同时恢复：可以继续调查路由器原先提供的 DNS 或运营商解析服务。

## 关于刷新 DNS 缓存

DNS 缓存保存近期使用过的解析结果。刷新缓存会让设备在下次访问时重新查询，但它不会：

- 修复 Wi-Fi 断开。
- 修复路由器没有上游连接。
- 修复网站服务器宕机。
- 提高宽带持续下载速度。
- 修复错误的代理或固定 IP 设置。

刷新 DNS 缓存通常不会删除 Wi-Fi 密码或网络配置，但不同系统版本的命令和权限要求可能变化。应优先使用操作系统厂商针对当前版本提供的说明。

如果无法确认命令是否适用于当前系统，可以先重启浏览器或设备，不需要反复执行来源不明的命令。

## 不建议做的事

- 不要只凭一个网站打不开就修改 DNS。
- 不要直接把整个家庭路由器作为第一次测试对象。
- 不要使用陌生网页提供的未知 DNS 地址。
- 不要同时修改 DNS、代理、固定 IP 和路由设置。
- 不要在单位、学校或受管设备上绕开管理员要求。
- 不要把“查询返回地址”理解为网站一定正常。
- 不要认为修改 DNS 可以提高宽带下载速度。

## 常见问题

### DNS 能解析出地址，为什么网页仍然打不开？

域名解析只是访问网站的第一步。目标服务器未响应、证书错误、浏览器配置、代理或网络路径异常，都会让后续连接失败。

### 改 DNS 会提高网速吗？

DNS 主要影响域名查询阶段，不能提高宽带线路的持续传输能力。解析响应变快可能让首次打开页面感觉更快，但不会消除带宽限制和网络拥塞。

### 应该在设备还是路由器上修改？

诊断时先修改一台个人设备，影响范围最小。确认长期需要，并了解对家庭内所有设备的影响后，才考虑修改路由器。单位或受管网络不应自行修改。

### `nslookup` 正常就能排除所有 DNS 问题吗？

不能。它只能说明这次查询得到了结果。浏览器安全 DNS、不同记录类型、缓存、特定子域名和间歇性故障仍可能产生不同结果，需要结合浏览器、设备和网络对照判断。

<div class="next-links" markdown="1">

[网页打不开排查]({{ '/website-not-opening/' | relative_url }}) [只有一台设备不能上网]({{ '/one-device-no-internet/' | relative_url }}) [Wi-Fi 已连接但不能上网]({{ '/wifi-connected-no-internet/' | relative_url }})

</div>

</article>
